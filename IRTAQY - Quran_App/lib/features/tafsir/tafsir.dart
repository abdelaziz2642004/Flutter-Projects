import 'package:flutter/material.dart';

class Tafsir extends StatelessWidget {
  const Tafsir({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tafsir Screen', style: TextStyle(fontSize: 24)),
    );
  }
}
